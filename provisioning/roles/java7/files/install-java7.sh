#! /bin/bash

#note: the rpm must be in the same directory as the Vargrant file 
# in this case, above provisioning/roles/java7/files (where this file is)
sudo yum -y install /tmp/jdk-7u75-linux-x64.rpm
